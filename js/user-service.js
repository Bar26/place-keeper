'use strict'


function setEmail(mail) {
    saveToStorage('mail', mail)
}

function setAge(age) {
    saveToStorage('age', age)
}

function setBcColor(color) {
    saveToStorage('bc-color', color)
}

function setTxtColor(color) {
    saveToStorage('txt-color', color)
}

function setBDate(bDate) {
    saveToStorage('b-date', bDate)
}

function setBTime(bTime) {
    saveToStorage('b-time', bTime)

}